{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary">
        <i class="fa fa-save"></i>
        {{ button_label|default('admin.control.save'|trans)|capitalize }}
    </button>
{{ form_end(form) }}