{% extends 'base.html.twig' %}

{% block title %}Profile{% endblock %}

{% block content %}
    <h1>Profile</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ profile.id }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ profile.content }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('management_profile_index') }}">back to list</a>

    <a href="{{ path('management_profile_edit', {'id': profile.id}) }}">edit</a>

    {{ include('management/profiling/profile/_delete_form.html.twig') }}
{% endblock %}