{% extends 'base.html.twig' %}
{% trans_default_domain 'admin' %}

{% block title %}Nouveau geste{% endblock %}

{% block content %}
    <h1>Créer un nouveau geste</h1>

    {{ include('thesaurus/gesture/_form.html.twig') }}

    <div class="help-links">
        <a href="{{ path('thesaurus_gesture_index') }}">
            <i class="fa fa-undo action-icon"></i>
            <span class="action-name">
            {{ 'admin.control.back_to_list'|trans|capitalize }}
            </span>
        </a>
    </div>
{% endblock %}