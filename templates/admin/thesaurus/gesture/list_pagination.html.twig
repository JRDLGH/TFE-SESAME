{% trans_default_domain 'admin' %}

<table class="table admin-table">
    <thead>
    <tr>
        <th>{{ knp_pagination_sortable(gestures,'admin.gesture.name'|trans,'g.name') }}</th>
        <th>{{ knp_pagination_sortable(gestures,'admin.gesture.profileVideo'|trans,'g.profileVideo') }}</th>
        <th>{{ knp_pagination_sortable(gestures,'admin.gesture.video'|trans,'g.video') }}</th>
        <th>{{ knp_pagination_sortable(gestures,'admin.gesture.cover'|trans,'g.cover') }}</th>
        <th>{{ knp_pagination_sortable(gestures,'admin.gesture.description'|trans,'g.description') }}</th>
        <th>{{ knp_pagination_sortable(gestures,'admin.gesture.publicationDate'|trans,'g.isPublished') }}</th>
        <th>{{ 'admin.control.actions'|trans }}</th>
    </tr>
    </thead>
    <tbody class="table-content">
    {{ include('admin/thesaurus/gesture/list.html.twig') }}
    </tbody>
</table>
{# display navigation #}
<div class="navigation">
    {{ knp_pagination_render(gestures) }}
</div>